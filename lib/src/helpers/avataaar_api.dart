import 'dart:ui';

/// Contains the baseUrl and color when the svg is fetched.
class AvataaarsApi {
  static const baseUrl = 'https://avataaars.io';
  static const baseBackgroundColor = Color(0xff65C9FF);
}
